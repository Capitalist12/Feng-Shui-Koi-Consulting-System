server:
  port: 8080
  servlet:
    context-path: /fengshuikoi
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/FengShuiKoiDB"
    username: root
    password: 12345
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        globally_quoted_identifiers: true